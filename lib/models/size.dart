class Size {
  int id; //deklarasi variabel id untuk data menu
  String name; //deklarasi variabel name untuk data menu
  bool isActive; //deklarasi variabel isActive untuk data menu

  Size({
    required this.id, //pemanggilan variabel pada kelas Size
    required this.name, //pemanggilan variabel pada kelas Size
    required this.isActive, //pemanggilan variabel pada kelas Size
  });
}
